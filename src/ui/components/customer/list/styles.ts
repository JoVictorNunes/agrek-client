import styled from 'styled-components';

const Actions = styled.div`
  display: flex;
  width: 100%;
  align-items: center;
  padding: 8px 0;
`;

const Container = styled.div`
  flex-grow: 1;
  padding: 8px 16px;
`;

const styles = { Actions, Container };

export default styles;
